# MMI Chain-of-Command Mission Map — machine-readable routing spine
# Authority: chain-of-command read → estimate → plan → execute (ALL_CLEAR delegable lanes)
# Owner: Matt Nichol · Matt §11 only on requires_matt_escalation waypoints
# Human view: mmi/MMI_MISSION_MAP.md
# Engine: python3 scripts/mmi_mission_map.py [--position]
# Re-staged: MMI-DEC-135 (2026-06-23) — Stage A platform credibility closeout + Stage B federation-first
# Breadth queue: BOR feedstock + Estimator + PMV after post_stage_c (MMI-DEC-182); not Matt hold fork

version: 1
map_id: mmi_chain_of_command_mission_map
north_star: Build the full 70-agent governed swarm organism — email fraud / inbox MDR is the first battlefield, not the whole identity.

roster:
  Cursor: Cursor
  Claude: Claude
  Codex: Codex
  Gemini+ChatGPT: Gemini+ChatGPT
  Matt: Matt

stages:
  - id: foundation
    name: Foundation — command spine + control plane GATED
    start:
      label: Command spine contracts signed (MMI-DEC-102 / MMI-DEC-098)
      complete_when:
        - type: decision_log_contains
          pattern: MMI-DEC-102
        - type: decision_log_contains
          pattern: MMI-DEC-098
    end:
      label: Routing-policy annex gate clean + spine wrappers GATED
      complete_when:
        - type: decision_log_contains
          pattern: MMI-DEC-123
        - type: file_contains
          path: docs/mmi/contracts/001_swarm_commander_routing_policy_annex.md
          pattern: "§11 SIGNED"
    waypoints: []

  - id: stage_a
    name: Stage A — platform credibility (GATED stack + docs)
    start:
      label: Organism concept captured (MMI-DEC-125)
      complete_when:
        - type: decision_log_contains
          pattern: MMI-DEC-125
    end:
      label: Real cyber platform credibility closeout (GATED stack + docs)
      complete_when:
        - type: decision_log_contains
          pattern: MMI-DEC-130
    waypoints:
      - id: a01
        label: Chain-of-command mission map engine + PMV wiring
        hand_to: Cursor
        you_do: >-
          Finish scripts/mmi_mission_map.py, staged YAML, PMV ALL_CLEAR relay,
          tests, and closeout MMI-DEC-126. Run lane_board_sync + mmi_dispatch
          --verify + pmv after commit.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-126
        requires_matt_escalation: false

      - id: a02
        label: Swarm Build Map reconciliation to mission map
        hand_to: Cursor
        you_do: >-
          Reconcile 4. Product_Roadmap/Swarm_Build_Map_Master.md one-screen lead
          to Stage A mission map (replace stale Shadow Layer 2 lead); add
          MISSION_MAP_RECONCILED banner; closeout MMI-DEC-127.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-127
        requires_matt_escalation: false

      - id: a03
        label: Terminology purge — fission → Lung Multiplier (buyer voice)
        hand_to: Claude
        you_do: >-
          Purge buyer-facing "fission" term in GTM/commercial docs; use Lung
          Multiplier vocabulary per MMI_OPERATOR_LUNG_DIAL_SPEC.md; record
          MMI-DEC-128.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-128
        requires_matt_escalation: false

      - id: a04
        label: MSP pilot motion intake (historical — superseded by re-stage)
        hand_to: Claude
        you_do: >-
          Historical intake filed MMI-DEC-129 (Todd path). Superseded as Stage A
          end gate by MMI-DEC-135 re-stage; not direction; not Todd-quiz milestone.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-129
        requires_matt_escalation: false

      - id: a05
        label: Mesh hardening research closeout (Phase B prep)
        hand_to: Gemini+ChatGPT
        you_do: >-
          Close open research items in
          mmi/concepts/MMI_IMMUNE_FEDERATION_MESH_HARDENING_ADDENDUM.md
          (pricing, copy caps, legal consent, protobuf); advisory memo only;
          closeout MMI-DEC-131. No mesh build authorization.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-131
        requires_matt_escalation: false

      - id: a06
        label: Stage A closeout — platform credibility (GATED stack + docs)
        hand_to: Cursor
        you_do: >-
          File Stage A platform credibility closeout inventory (GATED stack, signed
          contracts, docs posture); records Stage A complete (MMI-DEC-130). Not
          Todd buyer proof; not NorthStar naming; not build authorization.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-130
        requires_matt_escalation: false

  - id: stage_b
    name: Stage B — federation-first SMB seat economics + swarm program
    start:
      label: Stage A complete (MMI-DEC-130)
      complete_when:
        - type: decision_log_contains
          pattern: MMI-DEC-130
    end:
      label: Federation economics anchored + mesh contract path ready
      complete_when:
        - type: decision_log_contains
          pattern: MMI-DEC-140
    waypoints:
      - id: b01
        label: Federation-first SMB seat economics model
        hand_to: Gemini+ChatGPT
        you_do: >-
          Model federation pool seat economics for MSP SMB motion per hardening
          addendum §11; advisory memo only; closeout MMI-DEC-132. Lung production
          remains downstream of brain/immune + tenant data.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-132
        requires_matt_escalation: false

      - id: b02
        label: Brain/immune + tenant-data prerequisite audit (Lung prod gate)
        hand_to: Cursor
        you_do: >-
          Audit brain/HQ + immune GATED stack readiness vs Lung production
          prerequisites; tenant-data dependency inventory; closeout MMI-DEC-133.
          Lung prod blocked until this audit passes.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-133
        requires_matt_escalation: false

      - id: b03
        label: Immune Federation Mesh contract draft (research-gated)
        hand_to: Claude
        you_do: >-
          Draft signed-contract lane for Immune Federation Mesh using hardened
          addendum; CONCEPT → CONTRACT_DRAFT only; Matt §11 before build;
          closeout MMI-DEC-134.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-134
        requires_matt_escalation: false

      - id: b04
        label: Governed swarm program charter (70-agent motion)
        hand_to: Claude
        you_do: >-
          Charter federation-adjacent governed swarm program motion aligned to
          scoreboard; not Todd pilot; not buyer-proof quiz; closeout MMI-DEC-136.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-136
        requires_matt_escalation: false

      - id: b05
        label: Stage B end — federation economics + mesh contract fork
        hand_to: Matt
        you_do: >-
          Matt §11 on federation SMB seat economics model + mesh contract when
          ready (MMI-DEC-140). Not implied by draft lanes; not Todd-quiz gate.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-140
        requires_matt_escalation: true

  - id: stage_c
    name: Stage C — Lung production, federation scale, governed production loop
    start:
      label: Stage B complete (MMI-DEC-140)
      complete_when:
        - type: decision_log_contains
          pattern: MMI-DEC-140
    end:
      label: Self-evolving organism loop in governed production posture
      complete_when:
        - type: decision_log_contains
          pattern: MMI-DEC-150
    waypoints:
      - id: c01
        label: Operator Lung Dial wiring spec → signed contract
        hand_to: Claude
        you_do: >-
          Promote MMI_OPERATOR_LUNG_DIAL_SPEC.md to signed contract lane only
          after brain/immune + tenant-data prerequisites (MMI-DEC-133) satisfied;
          closeout MMI-DEC-141.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-141
        requires_matt_escalation: false

      - id: c02
        label: Federation mesh pilot wiring plan (opt-in MSP pool)
        hand_to: Cursor
        you_do: >-
          Draft federation mesh pilot wiring plan for opted-in MSP pool; no
          production promotion; no cross-tenant truth sharing; closeout
          MMI-DEC-142.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-142
        requires_matt_escalation: false

      - id: c03
        label: Stage C end — governed production organism loop
        hand_to: Matt
        you_do: >-
          Operator authorization for governed production organism loop
          (MMI-DEC-150). Lung production not implied without prior stages.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-150
        requires_matt_escalation: true

  - id: post_stage_c
    name: Post–Stage C — breadth GOVERNED_AGENT rollout
    start:
      label: Stage C complete (MMI-DEC-150)
      complete_when:
        - type: decision_log_contains
          pattern: MMI-DEC-150
    end:
      label: Breadth promotion queue cleared + chain-of-command synced
      complete_when:
        - type: decision_log_contains
          pattern: MMI-DEC-182
    waypoints:
      - id: br01
        label: Team 9 sandbox promotion chain (#61–#65)
        hand_to: Cursor
        you_do: >-
          Record GOVERNED_AGENT promotions #61–#65 (MMI-DEC-176–179); sync
          scoreboard + BOR; run lane_board_sync + mmi_dispatch --verify + pmv.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-179
        requires_matt_escalation: false

      - id: br02
        label: Phase 2 Layer 0 promotion batch (#72–#77)
        hand_to: Cursor
        you_do: >-
          Promote #72–#77 GOVERNED_AGENT per Phase 2 contract evidence;
          closeout MMI-DEC-180.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-180
        requires_matt_escalation: false

      - id: br03
        label: Phase 3 Layer 1 promotion batch (#78–#83)
        hand_to: Cursor
        you_do: >-
          Promote #78–#83 GOVERNED_AGENT per Phase 3 contract evidence;
          closeout MMI-DEC-181.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-181
        requires_matt_escalation: false

      - id: br04
        label: IFM mesh hardening audit closeout
        hand_to: Cursor
        you_do: >-
          Close IFM AUDIT lane with adversarial checklist (MMI-DEC-175);
          DESIGN lane COMPLETED per MMI-DEC-140 §11 + active-lanes patch.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-175
        requires_matt_escalation: false

      - id: br05
        label: Chain-of-command refresh — breadth milestone recorded
        hand_to: Cursor
        you_do: >-
          Sync MMI_MISSION_MAP.md v5, YAML post_stage_c stage, handshake,
          LAST_COMPLETED; closeout MMI-DEC-182; run lane_board_sync +
          mmi_dispatch --sync + --verify + pmv after commit.
        complete_when:
          - type: decision_log_contains
            pattern: MMI-DEC-182
        requires_matt_escalation: false
