# PROGRESS

**Purpose:** Always-current task tracker. Updated whenever a task is finished.

**Update rule:** When a task is closed, mark it ✅, add finish date and verification line, then move to the next item in the list.

**Runtime baseline (last verified):** **1247 tests passing, 1 skipped, 4 xfailed** (verified 2026-06-08, exit code 0). The +13 passing / +2 xfailed delta from the prior `1234 tests passing, 1 skipped, 2 xfailed` baseline is the Section 5 `AggregateCorroborationAgent` Evidence Stage 1 build: 13 passing Class 1 + Class 2 tests and 2 Class 3 known-gap `xfail` tests (KG-001 real-email verdict quality, KG-002 second-Challenge-agent cross-arbitration) in `tests/layer5/test_aggregate_corroboration_agent_section5.py`. The +106 passing delta from the earlier `1128 tests passing, 1 skipped` baseline is the 2026-06-07 governed-agent wrapper work (`tests/test_header_divergence_agent.py`, `tests/test_ghost_thread_agent.py`, `tests/test_email_authentication_agent.py` for the three GOVERNED_AGENT promotions #6/#8/#6A), the Layer 5 Section 2 aggregate-invocation test added 2026-06-08 in `tests/test_swarm_commander.py` (`test_layer5_challenge_receives_full_aggregate_set_once`), and the 15 passing Layer 5 Section 6 documented-failure tests in `tests/layer5/test_aggregate_challenge_section6.py` (Class 1 expected-pass + Class 2 adversarial + a disposition-unchanged guard); the 2 xfailed are the Class 3 known-gap tests (KG-001 / KG-002). The prior **1072 passing** baseline (2026-06-03) was the pre-Cyber-Insurance-stages count; the +56 delta is the stage 8/9/10 audit-packet + package-auditor + done-declaration modules, the criterion-14 operator-signature mechanism, the PDF renderer, and the pipeline integration test, all under `tests/test_cyber_insurance_*`. The +17 delta from `1055 tests passing, 1 skipped` is the Wave 3.1 score-sheet review ledger helper implementation tests in `tests/test_score_sheet_review_ledger.py`, covering the read-only review helper, shared scanner, separate score-sheet safety hook, stdout-only draft placeholders, stale reporting, no packet mutation, no secret/raw-value echo, and no `pre_ship_audit.py` import boundary. The +53 delta from the prior `990 tests passing` baseline is the Callback Phishing / TOAD detector pass 2 wiring + break-it tests (commit `9bcb3d5` — 15 new integration tests in `tests/test_callback_phishing_scoring_integration.py` covering default-off no-regression, enabled-fire path, body_plain-only enforcement, attach-always invariant, flag append, max-merge floor lift, signed rubric §11.2 floor-lift, §11.2 higher-band exact-2 branch, and production-loop rebuild preservation; 38 new adversarial tests in `tests/test_callback_phishing_break_it.py` covering false-positive resistance, false-negative resistance, scope-violation probes (no phone-number / no body_html / no numeric callback_phishing_score / no default-off leakage), crash resistance (empty / whitespace / control characters / zero-width unicode / >200K-char bodies / scan-cap probes), over-lift probes, rubric explanation mismatch probes, cross-tenant isolation, production-loop preservation, daily-digest D8 OOB wording rendering, and StrictModel schema integrity). The further +6 delta from `1043 tests passing` to `1049 tests passing` is the six TOAD scope-boundary / adversarial tests added 2026-05-31 in `tests/test_callback_phishing_break_it.py` under the §11-SIGNED TOAD spec's D11 (English-only closed phrase-category vocabulary) and D14 (`body_plain`-only input surface). The further +6 delta from `1049 tests passing` to `1055 tests passing` is the six Vendor Payment Integrity break-it tests added 2026-05-31 in `tests/test_vendor_payment_integrity_break_it.py` under the §11-SIGNED Financial State Ledger / Delta Tripwire spec (D9 risk floor 85 / D11 no autonomous payment decision / D13 data minimization), the Vendor Baseline Store spec (D2 hash-only / D5 per-tenant salt / D6 per-tenant file isolation), the Tiered Detection Intensity spec (D11 lift-only invariant), and the `CURRENT_STATE_MAP.md` Alert-fatigue doctrine — covering: (1) multi-signal `recommended_action` stays `needs_review` and never produces any block/quarantine/deny/reject verb even when three different first-seen payment signals stack inside one email; (2) raw routing / account / IBAN (spaced + packed) / SWIFT (spaced + packed) / payment-portal URL with `token=supersecret` query never leak into the full assessment `asdict` dump across the entire supported signal set; (3) benign vendor-payment language carrying `pay / remit / invoice / billing` context plus a bare 10-digit reference number without a labelled financial identifier produces zero extracted signals, zero findings, `recommended_action == "none"`, and opens no per-tenant Vendor Baseline Store row; (4) multi-finding FSL overlay max-merges the floor (LLM 10/50/84/85 lift to 85; LLM 86/95/100 preserved unchanged) and three findings do NOT stack additively above 85; (5) `daily_digest_agent` does not import `financial_state_ledger` / `FinancialStateLedgerAssessment` / `assess_financial_state_delta` / `DeltaTripwireFinding` directly (Alert-fatigue doctrine pinned in code: Stage A is one digest per `(tenant_id, digest_date)`, not a per-email FSL alert stream); (6) per-tenant HKDF salt yields different `signal_hash` values for identical raw routing numbers ingested into `tenant_break_it_a` vs `tenant_break_it_b`, both database files exist at distinct per-tenant paths, and the raw routing bytes appear in neither on-disk SQLite file. All six confirm stated boundaries within signed specs; zero runtime / detector / scoring / daily-digest / vendor-baseline-store code changed.

---

## Current handoff (updated 2026-06-08)
**Build Sequencer adoption (Option B) — SIGNED + LIVE.** Rubric `Next_Action_Decision_Rubric_Deep_Dive.md` §12 D13-rev SIGNED 2026-06-08 ("Matt Nichol June 8th 2026", placed verbatim; signed-spec slice gate-clean). Consequence Matrix `4. Product_Roadmap/_Build_Sequencer_Adoption_Consequence_Matrix.md`; queue **retired** as ordering authority; `agent_concepts/Blue_Team_Swarm_70_Agent_Scoreboard.md` v2 is the canonical candidate generator (Build Sequencer header, `BLOCKERS`/`TRACK`/`LAST_RUBRIC_SCORE`/`LAST_UPDATED` columns, BS-D1–D6 generator contract); `AGENTS.md` §3.2 Steps 0.5 (freshness check) + 6.5 (row update) in force. Authority chain LIVE: scoreboard generates -> rubric ranks -> Matt selects -> `decision_cycles_log.md` records. Gate automation for scoreboard freshness remains a separate future slice (Steps 0.5/6.5 doctrine-enforced). Nothing pushed. **Next swarm step: run a Build Loop cycle from the scoreboard Build Sequencer header — wrap the next actionable-now detector (e.g. #7 Sender Identity, #18 Callback Verification, #27 Link Inspection, #30 Attachment Risk) or build the second Layer 5 Challenge agent (closes KG-002).**

## Prior handoff (updated 2026-06-08)
**Section 5 — first real Layer 5 Challenge agent: Agent Design Contract §11 SIGNED (`0d7ff79`) and the Evidence Stage 1 build committed (`7256816`).** `4. Product_Roadmap/Aggregate_Corroboration_Agent_Design_Contract_Deep_Dive.md` is §11 SIGNED 2026-06-08 ("Matt Nichol June 8th", placed verbatim; spec-gate clean 0/0 pre- and post-signature). It is the first per-agent Agent Design Contract for a Layer 5 Challenge agent, drafted by the Claude advisory lane and reconciled against the real code by the execution lane (three accuracy refinements folded in: Authority Level 5 is above the Stage A Level-3 default and this signature is the authority decision per template §10.A Q3; the seven-field decision-evidence interface is conceptual, NOT a `DecisionEvidenceRecord` schema change; the registry entry pins `authority_level=5` + `role=AgentRole.BLUE` so the Commander metadata-match guard passes). §10.A locks Q1 Option B (cross-arbitration deferred — closes failure-register KG-002 as a named deferred decision until a second Challenge agent contract), Q2 Option A (D4/D5 enforced by contract + an independent Class 2 probe, no runtime self-validation), Q3 (`agent_id = aggregate_corroboration_001`). **Build** (committed `7256816`, build-gate clean 0/0 against the three signed contracts via worker manifest): `core/orchestrator/aggregate_corroboration_agent.py` — a deterministic, facts-only aggregate reviewer that reads the full per-case `tuple[AgentContribution, ...]` and returns one `ChallengeResult | None`; **confirms only when independent surfaces (header / authentication / thread) corroborate the same impersonation pattern** (D4 corroborate-not-vote-count — count-only verdicts never confirm, closing ADV-005 at the agent-contract layer), `inconclusive` on weak/non-overlapping facts, `contradicted` when verified-sender evidence opposes spoofing indicators. `authority_level=5`, Stage A, no autonomy; **not** in `build_default_registry` / production dispatch (explicitly-wired only at Evidence Stage 1). Three-class Section 5 suite in `tests/layer5/test_aggregate_corroboration_agent_section5.py` (Class 1 expected-pass, Class 2 adversarial incl. the vote-counting + count-language + over-contradiction + authority-level-mismatch probes, Class 3 known-gap xfail KG-001/KG-002): 13 passed, 2 xfailed. No detector logic, `_determine_disposition`, `DecisionEvidenceRecord` schema, or `build_default_registry` change. Full suite **1247 passed, 1 skipped, 4 xfailed** (1 warning is the pre-existing ReportLab deprecation, unrelated). Also this session: **`AGENTS.md` §2.1.1.B operator sign-off scope** (operator-stated 2026-06-08) — Matt signs off on the important locks only (§11 / Build Authorization / scope expansion / push / destructive git); the execution lane commits gated-clean in-scope work and updates trackers without per-step sign-off, reporting the hash after. Nothing pushed. **Next swarm step: Evidence Stage 2 promotion of `AggregateCorroborationAgent` is a separate later Matt-signed event (template §6.2 — ≥3 real contribution sets reviewed + a signed promotion record); KG-002 cross-arbitration stays deferred until a second Challenge agent contract. No new Challenge agent or registry registration without its own signed authorization.**

## Prior handoff (updated 2026-06-08)
**Layer 5 Section 6 test discipline committed `ade370e` — the signed spec's D7 promotion bar is now met.** Three test classes per the signed Layer 5 spec §6/D7 are committed and were gated clean (Grok 0 blocking / 0 warnings, audited against the signed spec via worker manifest) in `tests/layer5/test_aggregate_challenge_section6.py`: Class 1 expected-pass (aggregate challenge invoked once per case over the full set D1, verdict flows into `challenge_pass` D2, disposition precedence present/absent D5, `agent_id` guard, three Detection wrappers return `None` D6, `None` result excluded, multiple/empty `challenge_agents` paths); Class 2 adversarial/break-it (authority-drift blocked via metadata-mismatch D5, `agent_id` mismatch blocked, single-invocation-not-per-contribution D8, reasoning-trace absence confirmed D3, vote-counting probe documented as ADV-001 `partial` — enforcement deferred to the Section 5 Agent Design Contract per D4); Class 3 known-gap `xfail` (KG-001 real-email corroboration quality deferred to Stage 2; KG-002 multi-Challenge-agent cross-arbitration deferred to §10 Q1). Durable failure register on disk at `audit_outputs/failure_register/layer5_section6_failure_register.json` (git-ignored under `audit_outputs/`; ADV-001/KG-001/KG-002 completion paths recorded). Also committed in `ade370e`: **`AGENTS.md` §2.1.1.A anti-duplication lane rule** (operator-accepted 2026-06-08) — one live repo writer (execution lane), advisory code is design input not drop-in truth, plans are the preferred handoff, no duplicate rebuild after a clean gate, combine strengths by phase. Full suite **1234 passed, 1 skipped, 2 xfailed** (the 1 warning is a pre-existing ReportLab deprecation, unrelated). Nothing pushed. **Next swarm step: Section 5 — the first real aggregate Challenge agent. Spec-first; it requires its OWN §11-signed Agent Design Contract (Claude design lead, Matt signs) before any code. The contract must resolve ADV-001/D4 (prohibit count-only verdict reasoning as an enforceable agent-contract clause, since the Commander spine does not inspect verdict reasoning) and KG-002/§10 Q1 (define multi-Challenge-agent cross-arbitration, or explicitly defer with a boundary).**

## Prior handoff (updated 2026-06-08)
**Layer 5 Aggregate Challenge Pass spec §11 SIGNED (`1c4ccf6`) and Section 2 built / gated / committed (`50f3306`) — the aggregate challenge spine is now real, not theoretical.** The spec `4. Product_Roadmap/Layer_5_Aggregate_Challenge_Pass_Deep_Dive.md` is §11 SIGNED 2026-06-07 (signature "Matt Nichol June 7th 2026", placed verbatim; spec-gate clean 0/0). It governs the *mechanism* of the Pass-2 Layer 5 challenge pass and the `Agent.challenge()` contract signature; it promotes **no** Challenge agent. **Section 2** (committed `50f3306`, operator sign-off "Matt Nichol", code-gate clean 0/0 audited against the signed spec via worker manifest) is the locked change set under spec D1/D2/D6/D8: `Agent.challenge()` now takes the full per-case `tuple[AgentContribution, ...]` (aggregate review — ghost-thread + SPF fail + header divergence in one email reviewed together, not one contribution at a time); `SwarmCommander.run_case()` invokes each Layer 5 agent **once per case** over the full set, returning one `ChallengeResult | None` per agent; `_determine_disposition` precedence is unchanged; the three governed Detection wrappers (#6/#8/#6A) migrated to the new signature and still return `None` (D6); every challenge-agent test double + `.challenge()` call site migrated in the same change set with no split signature (D8); a new test proves once-per-case aggregate invocation. No detector logic / scoring / `analyze()` behavior changed; no Challenge agent registered, promoted, or added to `build_default_registry`; no autonomy. Full suite **1219 passed, 1 skipped**; lints clean. Nothing pushed (144 commits unpushed; push is operator-only). **Next swarm step (two legitimate moves): (a) Layer 5 Section 6 — the documented-failure test discipline + durable failure register (already authorized by the signed spec, no new signature); (b) Section 5 — the first real aggregate Challenge agent, which requires its OWN §11-signed Agent Design Contract before any build (na until Matt signs that contract — the spec authorized the mechanism only).**

## Prior handoff (updated 2026-06-07)
**Email Authentication Agent Design Contract §11 SIGNED 2026-06-07 ("Matt Nichol June 7th 2026") and committed `23e35e8` — #6A is now a GOVERNED_AGENT at Evidence Stage 1 (Synthetic).** Third detector wrapped + governed on the proven path. Runtime proof wrapper `EmailAuthenticationAgent` (`core/orchestrator/email_authentication_agent.py`) + 13 focused tests committed `8e3df20`, wrapping the immutable `score_email_authentication` over `Authentication-Results` (SPF/DKIM/DMARC). Locks D1-D9: facts-only closed-set failure indicators (no score, no `from_domain`, no `*_result`); `email.sender`+`email.headers` input surface (detector reads only `Authentication-Results`; `sender` only derives `from_domain`, not emitted); not in `build_default_registry`; Stage A lift-only, no autonomy; §5 names the "pass means safe" fallacy. §10 Q1 resolved at signing: Email Authentication earns its **own scoreboard row (#6A)**, split from #6's code evidence (pre-wrapper classification artifact). Draft + signature gates clean 0 warnings. Tracker commit: scoreboard #6A row added + GOVERNED_AGENT tally 3->4, MASTER_INDEX entry, this handoff. **Next swarm step (operator-confirmed sequence): wire the Layer 5 challenge pass against the AGGREGATE AgentContribution set from all three governed detectors in one case — NOT one contribution at a time. Operator design constraint to hold: a Challenge agent must review the full per-case contribution set (e.g. ghost-thread + SPF fail + header divergence in one email is a different signal than any alone). A fourth wrapper comes only after the challenge pass is proven.**

## Prior handoff (updated 2026-06-07)
**Ghost Thread Agent Design Contract §11 SIGNED 2026-06-07 ("Matt Nichol June 7th 2026") and committed `275475b` — #8 is now a GOVERNED_AGENT at Evidence Stage 1 (Synthetic).** `4. Product_Roadmap/Ghost_Thread_Agent_Design_Contract_Deep_Dive.md` governs the existing `GhostThreadAgent` wrapper around the immutable `score_ghost_thread` detector. Code-accurate after review at `67a21ee`: the detector keys on fake thread continuity (Re/Fw/Fwd subject prefix + missing non-empty `In-Reply-To`/`References`), NOT Reply-To divergence (that stays with #6 Header Analysis). Locks D1-D9: facts-only `ghost_thread_subject` (no score emitted); `email.subject`+`email.headers` input surface; registry-gated `AGENT_CONTRIBUTION`; not in `build_default_registry`; Stage A / no autonomy; D9 names the Stage 2 edge-case coverage gap as a known, accepted Stage 1 precondition. Signed-spec gate clean 0 warnings (`audit_outputs/ghost_thread_agent_contract_draft_20260607_20260608T025710Z.md`); focused pytest 59 passed; lints clean. Tracker commit renamed scoreboard #8 "Reply-To Mismatch" -> "Ghost Thread Agent" (§10 Q1). **§11.A REVISION SIGNED 2026-06-07 and committed `82c7c86`: §10.A resolves all four §10 questions** — Q1 RESOLVED (rename `fbdbe72`); Q2 Option B (explicitly-wired through Evidence Stage 2, default registry / live dispatch only at Stage 3); Q3 defer-with-boundary (Layer 2 never emits the numeric score under any condition; any Verification-layer use authored in the Verification-layer spec only, no amendment here); Q4 Option A (one composite `ghost_thread_subject` fact). Revision changed no detector logic, input surface, emitted facts, or Stage 1 behavior; re-gated clean 0 warnings pre- and post-signature. Next swarm step: wrap the next existing detector via the proven analyze -> contribution -> challenge path.

**Header Analysis Agent Design Contract §11 SIGNED 2026-06-07 ("Matt Nichol June 7th 2026") and committed `8f0a89f` — #6 is the first FULL governed agent.** `4. Product_Roadmap/Header_Analysis_Agent_Design_Contract_Deep_Dive.md` promotes #6 Header Analysis `DETECTOR_FUNCTION` -> `GOVERNED_AGENT` at Evidence Stage 1 (Synthetic), governing the tested `HeaderDivergenceAgent` wrapper around the immutable `score_header_divergence` detector; locks D1-D9; first per-agent contract under the signed template and first Evidence Stage application; not in `build_default_registry` / no production dispatch. Gate-clean at draft and re-gated clean at signing (spec-only gate, 0 warnings). Next swarm step: wrap the second existing detector via the proven analyze -> contribution -> challenge path.

**Swarm spine Layer 5 challenge pass built (2026-06-07, runtime/test commit `5d70040`).** Following the Header Divergence governed-wrapper proof, `SwarmCommander.run_case()` now accepts separate Layer 5 `challenge_agents`, validates them through registry metadata + Stage A/autonomy router guard, runs `challenge()` against real `AgentContribution` objects, writes results into the in-memory `DecisionEvidenceRecord.challenge_pass`, and lets the existing conservative disposition rules route contradicted challenges to `human_required` and inconclusive challenges to `hold`. Focused verification: `tests/test_swarm_commander.py tests/test_header_divergence_agent.py` -> 26 passed; lints clean; code gate clean 0 warnings. The Commander still does not persist DERs. Header Analysis is now `GOVERNED_AGENT` at Evidence Stage 1 via commit `8f0a89f`. Next swarm step: second existing detector wrapper.

**Cyber Insurance Evidence Package deep-dive: §13 SIGNED and committed (`85f2069`); §13 buyer-brand boundary revision RE-SIGNED 2026-06-05.** D1-D11 locked. **D10 is OVERRIDDEN, not met** — signed operator override; basis is ad-hoc confirmation signal in `Frontier_Intake_Log.md`, not validated market proof. The §2 required boundary statement now uses the external/commercial product name **Mutant Monkey Inbox Shield** on buyer-facing package surfaces; Matt authored the re-signature wording: "Approved by Matt Nichol, Sovereign Operator." "NorthStar Inbox Shield" remains the internal codename per rebrand Option B; no blanket repo rename.

**Cyber Insurance implementation spec: §11 SIGNED and committed (`4308b22`).** Matt authored the §11 signature in-chat as `Matt Nichol (Zebra-Comit) June 3rd. 2026`; the assistant placed it. Signed-spec Grok slice: **0 blocking / 0 warnings** at `audit_outputs/cyber_insurance_section11_spec_signature_slice_20260603_20260604T001901Z.md`. Build-layer decisions §3-§16 are locked, including IQ1-IQ7 and the §13 pins (Grok-4 / temp 0 / dedicated PDF toolchain / `vf-001` + legit contrast pair). Signing did not authorize code beyond later operator-selected build slices.

**§14 runner-first milestone EXECUTED / PASS (2026-06-04T00:49Z, live `grok-4`).** Matt selected Action A and clarified this is internal evidence-building only, not buyer-readiness. An inline runner was generated outside the repo at `/tmp/cyber_insurance_section14_runner.py` per the signed implementation spec. Fake-mode smoke first failed because the runner read `risk_score` from the wrong payload path; corrected inline; fake mode then passed; live `grok-4` run passed. Evidence lives under `audit_outputs/cyber_insurance_v1_test_plan/stage_a_vendor_payment_redirect_001/`: `run_summary.md`, `verdict.json`, `detection.json`, `verification.json`, `evidence.json`, `audit_trail.json`, `outcome_documentation.md`, `scoring_raw_response.json`. Verdict: all seven §14.4 conditions passed, all nine §7 gates passed, zero drift incidents. This discharges the runner-first / Done Criterion 15 evidence requirement for the fictional Stage A case; it is not buyer-facing output, not D10 market proof, and not full package-generator completion.

**Cyber Insurance gate library + generator Pass 1 COMMITTED (2026-06-04).** Two commits on `safety/queue-drift-cleanup-20260528`: gate library `db32cd5` (fresh Grok gate clean) and generator + CLI `ff0f41e`. The generator commit went through `complete_gate.py --operator-override` because the §11-signed implementation spec is a no-code document; Matt explicitly authorized generator Pass-1 start-build (internal scope) on 2026-06-04 — this supersedes the earlier "keep momentum" framing, which did not meet the spec bar. Implemented `core/evidence_package/` (nine §7 gates + Markdown-first generator) and `scripts/cyber_insurance_package_generate.py`. Verification: focused pytest **16 passed** (11 gate + 5 generator); live CLI all nine gates passed. Internal Pass 1 only: no PDF, no live Grok package audit, no done declaration, no package-level operator signature, no buyer-facing release. A warning-level drift incident records the override (`audit_outputs/drift_incidents/`).

**Wave 3.1 score-sheet review ledger helper:** implemented, Grok-clean, committed (`24a1498`) with scanner, helper, hook, tests, and 1072/1 full-suite baseline.

**Next-Action Decision Rubric §11 SIGNED (2026-06-04).** Matt authored the signature and instructed sign-off. Per the spec's own §9 + AGENTS §5, `complete_gate.py` ran first and returned clean (grok-4, 0 blocking / 0 warnings, "comprehensive" evidence; `audit_outputs/next_action_decision_rubric_signoff_20260604T062334Z.md`). The 7 §10 questions locked as D13-D19: hybrid option source, informal operator modes, dedicated `decision_cycles_log.md`, 14-day-retro + ≥3-streak calibration, Grok-on-FAIL + operator opt-in, compressed-prompt deferred one trial cycle, reference-scored "do nothing" on the same five axes. `decision_cycles_log.md` created (D15); MASTER_INDEX drift gap closed (the rubric had no prior index entry). The rubric is now the live tactical decision engine — it ranks, Matt selects. Also this session: the AGENTS.md decision-calibration guardrail (§3.1.2 substance-graded escalation, §3.1.8, and the §12 "trivia-escalation / decision-inversion" failure mode). Signature reads `Matt Nichol (Zebra-Comet) June 4th. 2026` (the initial "Nchol" typo was corrected to "Nichol" on Matt's explicit 2026-06-04 instruction).

**PDF render surface COMPLETE (2026-06-04).** IQ2 (already RESOLVED in the §11-signed impl spec as "dedicated pinned PDF dependency") had its concrete engine pinned with the operator: **ReportLab 4.2.5** (pure-Python, no system binaries) — chosen on a pre-scored comparison because it is the smallest cross-platform supply-chain surface to pin/test/audit, the exact dimension IQ2 flagged. New `core/evidence_package/pdf_renderer.py` renders a byte-deterministic (reportlab invariant) internal PDF per package: §2 boundary statement verbatim (HC8), package metadata, evidence-records table, determinism pins, plus a `rendered/pdf_render.json` sidecar carrying the pinned engine identity+version (HC6) and pdf_sha256. It is a separate explicit step (not auto-wired into generation), synthetic/internal-only, **no buyer delivery**; engine-pin mismatch and empty boundary fail closed. Grok gate clean 0/0 (`audit_outputs/cyber_insurance_pdf_render_surface_20260604T193751Z.md`); **1128 passed, 1 skipped**; committed under STANDING. Gated remainders: real-customer-data controls decision (before any non-synthetic Grok submission or buyer render) and buyer PDF delivery.

**Cyber Insurance buyer-brand boundary revision COMPLETE (2026-06-05).** Buyer-facing package boundary and MSP retainer summary now name **Mutant Monkey Inbox Shield** / **Mutant Monkey Security**. Regenerated and re-rendered the synthetic Bluefin package; visual inspection confirmed the PDF no longer exposes "NorthStar" on the boundary statement. Full suite **1128 passed, 1 skipped**. Gate clean 0/0 after Matt's operator-authored re-signature (`audit_outputs/cyber_insurance_buyer_brand_boundary_20260605T005903Z.md`). No buyer delivery opened; synthetic/internal-only boundary remains intact.

**Real-customer-data audit-model intent logged (2026-06-04).** Matt clarified the intended path for the real-world data crossroads: real customer packages should move toward NorthStar's own AI running on a locked operator-controlled machine, not Grok/xAI. Grok remains acceptable for synthetic/test package audits while tokens remain available and while the operator still considers it safe; retire it when tokens run out or if it stops being safe. This is an operating-intent log, **not** a signed-spec change: current §13/IQ3 still pins `grok-4` / temp 0 for the synthetic/test audit path. Before any real customer package is audited or buyer-rendered, run the real-customer-data controls Consequence Matrix and, if changing the audit model, revise §13/IQ3 through the normal gate + operator sign-off path.

**Mutant Monkey package-audit brief authored (2026-06-05, milestone C2).** Wrote the concrete D11 artifact from the §11-signed controls spec: `4. Product_Roadmap/Mutant_Monkey_Package_Audit_Brief.md` (operational brief v1, pre-deployment). Defines the local-AI package auditor as the package-level mirror of `complete_gate.py` — independent negative-feedback reviewer that never audits its own build, audits a package against the signed §13/§11 specs, the controls contract, and the claim-boundary spec (`Compliance_and_Trend_Watch_Process.md`), enforces hard boundaries (real data never leaves the locked machine, binary synthetic/real classification, no rubber-stamp, no claim drift, boundary statement intact, tenant isolation), emits a Stage-9-parity timestamped audit artifact + Drift Incident Reports for Done Criteria 11/12, uses `clean`/`warnings_only`/`blocked` verdicts, and must pass a D9 calibration gate (match external baseline, catch planted defects, refuse broken packages) before any real-package use. Operator-facing wording is **Mutant Monkey** per Matt's instruction; signed-spec D11 internal codename kept as a traceability note only (signed spec not edited). Gate clean 0/0; committed `6026deb`. **Pre-deployment:** authorizes no infrastructure, no real-data handling, no §13/IQ3 change; governs synthetic calibration only until substrate + D9 pass + §13/IQ3 re-sign + operator activation.

**Real-customer-data controls spec §11 SIGNED (2026-06-05).** Consequence Matrix outcome was Option B; the seven §10 questions resolved to D9-D15; a §11-readiness gate ran clean; then Matt authored the §11 signature ("Matt Nichol(Zebra-Comet) June,5th. 2026", assistant placed only) and a fresh signed-spec gate ran clean 0/0. `Real_Customer_Data_Controls_Deep_Dive.md` now locks D1-D15: real data never to an external model, external model synthetic/test only with sunset, builder/auditor separation, separate production datastore, real-path controls at least as strict as synthetic, Stage-9-parity local audit output, §13/IQ3 revision precondition, buyer delivery separately gated, calibration gate over a fixed model, verifiable locked-machine controls, Dax-with-NorthStar-audit-brief default, separate Production Evidence Store spec, and explicit Grok/xAI synthetic sunset triggers. Committed `e77f85c`. **Signing locks the contract only** — it authorizes no infrastructure, no §13/IQ3 pin change, no real-customer-data handling, and no buyer delivery; each remains a separate explicit operator gate.

**Executive Impersonation Detector (swarm agent #21) §11 SIGNED (2026-06-06) by Matt Nichol** ("Matt Nichol June 6th 2026", placed verbatim). Milestone B (promote the next swarm-map agent) selected; drafted `4. Product_Roadmap/Executive_Impersonation_Detector_Deep_Dive.md` following the proven Lookalike path. Pure-function/offline detector for the executive-impersonation BEC pattern: claimed executive principal (display name) + identity mismatch (foreign domain / free-mail / look-alike consumed from the Lookalike detector) + closed-vocabulary authority/secrecy/urgency/task-directive pressure (TOAD pattern-engine style, new sibling module). Reuses `extract_identity_domains`, the per-tenant Vendor Baseline Store roster pattern, and the `LookalikeDomainAssessment` schema shape. D1-D9 candidate-locked; §10.A locks all seven questions with FP-safe, reuse-first defaults (per-tenant roster, normalized-exact match only in v1, new English-only sibling module, non-additive bands weak≤25/probable 70/strong 85, default-off until signed calibration, From+Reply-To+display-name+`body_plain` surface + curated free-mail list, `sender_identity` evidence-attribution-only). Operator refinements applied: Q1 privacy wording corrected (plaintext names are a deliberate tradeoff; salt protects only derived identifiers, not plaintext; protection is per-tenant storage isolation); Q7 rubric-boundary guard (flag is evidence attribution only — must not create a new axis-score lift or point-structure rule in v1); §11 softened to "stable engineering blueprint for a future build slice" with an explicit no-code-until-Build-Authorization clause. Spec draft banked (`d49f4c8`); §11 signature lands as its own gated slice. Signing locks D1-D9 + §10.A as the contract — no code, no wiring, no default-on, no rubric change authorized; a separate explicit Build Authorization is required before implementation.

**Canonical swarm design fit ADOPTED (2026-06-06).** Matt confirmed the 6-layer articulation fits: Mutant Monkey Security is a governed agentic evidence swarm, not just an email scanner. `agent_concepts/Mutant_Monkey_Blue_Team_Swarm_Design_Tree.md` is now the **ADOPTED CANONICAL DESIGN MAP** for agent design and milestone shaping; the v1 70-agent map remains preserved as the original inventory/backlog cross-map. Canonical layers: Command, Detection, Verification, Evidence, Challenge/Red-Team, Learning/Governance. Governing rules: no trust decision without evidence; no high-risk action without verification; no failure without a learning record; **the detector is not the decision; the detector is the trigger for verification.** This is authority-free and builds nothing. **Next recommended milestone:** Agent Design Contract template (spec-only), then light #10/#21 retrofit to declare layer, authority level, two-pass role, decision-evidence-record contribution, and promotion/demotion fields without changing their signed detector contracts.

**Agent Design Contract Template §11 SIGNED (2026-06-06) by Matt Nichol** ("Matt Nichol June 6th 2026", placed verbatim). Created `4. Product_Roadmap/Agent_Design_Contract_Template_Deep_Dive.md` as the governance/template contract that turns the canonical 6-layer swarm design into required fields for future promoted agents. Candidate D1-D10 lock: template identity, canonical 6-layer map, detector-not-decision rule, authority declaration, evidence-first output, two-pass fit, promotion/demotion conditions, Stage discipline, metadata-only retrofit posture for #10/#21, and claim-safe boundary. The required block covers layer, role, authority level, boundary, evidence requirements, data minimization, tenant isolation, two-pass role, decision-evidence-record contribution, human/verification triggers, scoring/action posture, default rollout, autonomous-action status, promotion/demotion, retest evidence, calibration, failure modes, tests, audit requirements, signed-spec dependencies, and Build Authorization dependency. §10.A locks seven defaults: enforcement before §10 resolution; immediate metadata-only #10/#21 retrofit after sign-off; Stage A detectors default Level 3 Specialist; seven-field decision evidence record; promotion/demotion spec-only in v1; pure detectors may declare Pass 1 only until the Two-Pass spec signs; v2 design tree canonical / v1 map inventory-only. **Pending:** Matt's operator-authored §11 signature, then gate/commit. Draft authorizes no implementation, no runtime enforcement, no #10/#21 retrofit, and no new agent behavior.

**Production Evidence Store §11 SIGNED (2026-06-05) by Matt Nichol.** Rolling forward from the §11-signed controls spec (D12), Matt locked the recommended defaults for all five §10 open questions into a new §10.A Operator-Confirmed Decisions block in `4. Product_Roadmap/Production_Evidence_Store_Deep_Dive.md`: Q1 same operator host with an isolated MinIO-class instance + `mms-` namespaces (dedicated production host deferred to a later gated revision); Q2 **per-tenant credentials from day one** (no shared all-tenant credential — eliminates tenant-bleed at the root); Q3 indefinite-until-explicit-delete for evidence-bearing classes (source inputs, package bundle, audit packet, local-AI audit output, operator signature record) with no time-based auto-expiry, rendered PDFs internal-only; Q4 operator-controlled off-site media only (weekly + immediate post-package mirror, never consumer cloud); Q5 read-only-from-locked-machine + fixed egress-deny checklist in v1 (air-gapped copy workflow deferred). Spec status header set to "§11 SIGNED (2026-06-05) by Matt Nichol." Matt authored the §11 signature ("Matt Nichol", assistant placed verbatim only). Spec-only; no code, no infrastructure. **Committed + pushed** in four gate-clean slices (`6aae6b9` spec, `770075b` index+handshake, `7bb5d25` activity log, `e5c3672` progress); GitHub + local backup current through `e5c3672`; working tree clean. §11 sign-off does not authorize infrastructure — a separate explicit start-build instruction remains required. Optional next milestone: Local-AI audit substrate planning (spec-only).

**Decision-routing protocol trial + blue-team swarm map captured/adopted (2026-06-05).** `DECISION_PROTOCOL.md` is in TRIAL (committed `6a83bfc`, not yet adopted into AGENTS.md authority): Bin 1 (technical/reversible) the agent decides silently; Bin 2 (operator-authority/irreversible/money/identity/legal/real-data/buyer-delivery/butterfly/non-negotiable) always reaches Matt with positives/negatives/consequence/recommendation/why-on-demand; sessions open by setting a daily milestone list; a challenge guard checks every instruction against signed specs/non-negotiables/forbidden language/butterfly triggers before building. Then Matt's 70-agent blue-team swarm architecture was captured as a SPARK (`agent_concepts/_Blue_Team_Swarm_Architecture_Map_SPARK.md`, authority-free, builds nothing, expands no signed scope), cross-mapped (each agent EXISTS/SPECCED/GOVERNANCE/NET-NEW), and operator-adopted as the Stage B/C architecture map and daily-milestone backlog source — adoption is not build authorization. **Superseded as canonical design source on 2026-06-06 by `agent_concepts/Mutant_Monkey_Blue_Team_Swarm_Design_Tree.md`; preserved as the original inventory/backlog cross-map.** A new `agent_concepts/` folder (with README defining RAW vs ADOPTED states + promotion rules) is the operator's design-dump home for future build theories. Gates clean 0/0; committed `93822dd` then `48a5ce5`. The 2026-06-05 cleanup pass corrected this PROGRESS runtime baseline header (was a stale 1072) to the true **1128 passed, 1 skipped**.

---
## Active Task List

All prior task items through 2026-06-03 are completed / signed / deferred history; none are open. The detailed item-by-item list (58 items + Completed Recent History) was moved verbatim to `PROGRESS_ARCHIVE_2026-06-03.md` on 2026-06-03 to keep this tracker within the audit-gate packet cap (200KB / 50KB per file). Live state is in **Current handoff** above; source-of-truth files are below.

---

## Source-of-Truth Files

- `VISION.md` — long-arc product thesis (Stage A / B / C) and non-negotiables
- `MILESTONE_ARC.md` — multi-year milestone tracker with "done when" criteria
- `THREAT_INTEL_LOG.md` — record of threat patterns ingested and policy updates that resulted
- `REVENUE_MAP.md` — three-lane funding plan (Survival / Freelance / NorthStar) with Kelowna specifics
- `PROJECT_HANDSHAKE.md` — active build track and Completed log (items 168–178)
- `PROJECT_ACTIVITY_LOG.md` — chronological session entries
- `MASTER_INDEX.md` — repo index
- `think_sheet.md` — idea scoring / drift filter with Foundation Fit rubric and promote/live/deep park bands
- `4. Product_Roadmap/Vendor_Baseline_Store_Deep_Dive.md` — signed spec for the per-tenant Vendor Baseline Store; implementation landed 2026-05-24
- `3. SwarmCommand_Engine/Agent_Loop_Runtime/Runtime_Implementation/core/production_state/vendor_baseline/` — per-tenant SQLite Vendor Baseline Store implementation
- `PROGRESS.md` — this file (always update when a task finishes)
- `4. Product_Roadmap/Product_Sheets/Fraud_Detection_Product_Sheet.md` — customer-facing sheet + SMB tier matrix
- `3. SwarmCommand_Engine/Agent_Loop_Runtime/Autonomous_Triggers/autonomous-defensive-triggers.md` — trigger packet contract
- `3. SwarmCommand_Engine/Agent_Loop_Runtime/Runtime_Implementation/scripts/project_trigger_scan.py` — scanner implementation
- `3. SwarmCommand_Engine/Agent_Loop_Runtime/Runtime_Implementation/scripts/acme_effective_parameter_report_demo.py` — real Acme demo report generator
- `1. Business_Operations/Client_Documents/Generated/Acme_Effective_Parameter_Report_Demo.md` — generated MSP-facing effective-parameter report demo
- `3. SwarmCommand_Engine/Agent_Loop_Runtime/Runtime_Implementation/scripts/inbox_shield_daily_digest_demo.py` — runnable Inbox Shield five-email daily-digest demo generator
- `1. Business_Operations/Client_Documents/Generated/Inbox_Shield_Daily_Digest_Demo.md` — generated MSP-facing daily digest demo
- `1. Business_Operations/Client_Documents/Inbox_Shield_Sample_Monthly_Report.md` — current MSP-facing monthly review sample for Inbox Shield
- `3. SwarmCommand_Engine/Agent_Loop_Runtime/Runtime_Implementation/scripts/cisa_kev_ingest.py` — operator-run CISA KEV threat-intel ingestion (v0 of MILESTONE C1)
- `3. SwarmCommand_Engine/Agent_Loop_Runtime/Runtime_Implementation/eval_report_2026_05_22_phase_1_5_rerun.md` — latest Phase 1.5 `grok-4` rerun result
- `3. SwarmCommand_Engine/Agent_Loop_Runtime/Runtime_Implementation/core/scoring/email_risk_scoring_agent.py` — current locked scoring prompt

---

## Last Updated

2026-05-27 — Drift cleanup pass. Both 2026-05-26 active drift signals cleared. (a) Added `_SPARK_Bibles_Concept_Capture.md` to `MASTER_INDEX.md §4.3 Roadmap Docs` with explicit SPARK-only / pre-spec / unsigned / not-§11 status note (held, not buried; cross-referenced from `PROJECT_HANDSHAKE.md` 2026-05-25 bibles deferral entry). (b) Reconciled baseline-tests tracker: pytest verification today returned **946 passed, 1 skipped** in 12.03s; updated `PROJECT_HANDSHAKE.md` Current Active Build Track baseline from `688 passed` to `946 tests passing, 1 skipped (verified 2026-05-27)`; updated `PROGRESS.md` Runtime baseline header from `905 passed` to `946 tests passing, 1 skipped (verified 2026-05-27)`. Phrasing change is regex-aware: `_TRACKING_BASELINE_REGEX` in `scripts/project_trigger_scan.py` matches `\d+ tests? passing` and uses `.search()` first-hit semantics, so historical text in `PROJECT_HANDSHAKE.md` item #166 (`443 tests passing`) was previously winning over more-recent state. Putting the current-state line in matching format earlier in the file makes the scanner read current state first; historical item #166 left untouched (its `443` is accurate as of that entry's date). Verified by re-running `python -m scripts.project_trigger_scan --baseline-tests 946`: returned `scan_clean`, `drift_findings: []`, `baseline_tests_expected: 946`, `baseline_tests_recorded: 946`. Doc-only; no runtime change.

2026-05-26 — End-of-day drift snapshot + audit-the-auditor signal logged + Next-Action Build System v1 design captured (no commit; chat-only). Project trigger scan returns 2 active drift signals: (a) `MASTER_INDEX.md` does not list `4. Product_Roadmap/_SPARK_Bibles_Concept_Capture.md` even though `PROJECT_HANDSHAKE.md` references it; (b) baseline-tests tracker mismatch `expected=555 recorded=443` requires reconciliation. Audit-the-auditor signal observed across the four §12 resolution audits today: same packet shape (single touched file, no worker manifest), three different Grok evidence-quality framings — Q1/Q2 "comprehensive," Q3 "comprehensive," Q4 "partial because the manifest was absent." Not rubber-stamping; real run-to-run variance, surfaced because the hardened evidence-quality clause is now mandatory. Worker-manifest path (`audit_outputs/pending/<task_id>.manifest.json`) currently empty across all runs; gate honestly reports manifest absence in packet. Next-Action Build System v1 design captured in chat (5-axis scoring engine — Leverage / Risk / Evidence / Future Cost / Reversibility — that generates and ranks 3-7 options without deciding); not yet spec'd; flagged for spec-first treatment as `4. Product_Roadmap/Next_Action_Decision_Rubric_Deep_Dive.md` once operator chooses to lock five clarification items first. Doc-only; no tests required.

2026-05-26 — Cyber Insurance Evidence Package §12 Q4 resolved (commit `e2087c7 resolve cyber insurance Q4 carrier-agnostic format`). D4 locked: single carrier-agnostic format only — no per-carrier variants in v1, v1.1, or any later scope; this is product policy, not a deferred backlog item; reopening requires a new signed spec. D4 locked as structural constraint only. Q6 (vocabulary-translation list) and Q8 (forbidden-language list) remain independent filters under §9; not bundled with D4 under a unified governance frame. Renderer ordering between §7 gates is implementation-spec detail, not §6 / §9 doctrine. Operator rejected the proposed §9.1 framework (HC9 disclaimer anchor + A4 regulatory escape clause) before encoding. Gate audit returned `0 blocking / 0 warnings` at `audit_outputs/cyber_insurance_q4_carrier_agnostic_resolution_20260527T043134Z.md`. Doc-only; no tests required.

2026-05-26 — Cyber Insurance Evidence Package §12 Q3 resolved (commit `cacb344 resolve cyber insurance Q3 delivery surfaces`). D3-D3a locked: v1 ships two delivery surfaces only — PDF artifact (buyer-facing) and Markdown bundle (audit / engineering companion). Branded landing page and evidence vault with signed URL deferred to v1.1+ pending cheaper-proof MSP demand evidence (Q10). New §6.2 "Delivery surfaces" added. Operational commitments acknowledged: HC6 toolchain pinning, HC7 bundle format specification, HC8 minimal branding rules — all to be encoded in the implementation spec. Gate audit returned `0 blocking / 0 warnings` at `audit_outputs/cyber_insurance_q3_delivery_resolution_20260527T040700Z.md`. Doc-only; no tests required.

2026-05-26 — Cyber Insurance Evidence Package §12 Q1/Q2 resolved (commit `f1c062d resolve cyber insurance cadence and freshness policy`). D1-D2e locked: cadence is hybrid (quarterly snapshot + on-demand regeneration + annual full review anchored to tenant insurance-renewal anniversary); per-category freshness thresholds locked (30 days for `policy_change_control` / `tenant_isolation` / `kill_switch`; 90 days for `detection_evidence` / `scoring_explanation` / `test_evidence` / `independent_audit` / `operational_artifact`; structural exemption for any record with a valid `signed_by` reference). Supersession annotation rule locked: superseded signed artifacts remain freshness-exempt as historical signed evidence, but rendered package must annotate `superseded by <new spec>`. New §6.1 "Freshness policy" added. Operational commitments acknowledged: monthly internal verification path for 30-day categories, annual full review as new spec requirement, stale-evidence pre-warning behavior, queued on-demand concurrency for the same tenant, Grok API availability/cost as operational dependency. Gate audit returned `0 blocking / 0 warnings` at `audit_outputs/cyber_insurance_cadence_threshold_resolution_20260527T030754Z.md`. Doc-only; no tests required.

2026-05-26 — Compliance and Trend Watch Process spec §11 SIGNED + governance overhaul + gate prompt evidence requirement hardened + audit-the-auditor cadence wired into operating doctrine + Frontier Intake Log supersession (commits `470714d governance overhaul: sign compliance/trend-watch spec, add audit gate and build queue`, `69419fc harden gate prompt evidence requirement, add audit-the-auditor cadence, intake-log supersession`, `3c1e156 harden Grok gate evidence requirements and add audit-the-auditor cadence`). `4. Product_Roadmap/Compliance_and_Trend_Watch_Process.md` §11 signed by Matt Nichol after sharp Grok audit returned clean (`audit_outputs/compliance_trend_watch_signoff_sharp_20260527T004134Z.md`). Added: `audit_tools/complete_gate.py` (manifest-verified Grok negative-feedback completion gate; `OUTPUT_FORMAT_INSTRUCTION` now requires named-evidence on zero-finding outputs, insufficient-context escape hatch, and explicit Evidence quality paragraph before `GATE_SUMMARY`; locked `NEGATIVE_FEEDBACK_PROMPT` untouched), `audit_tools/run_compliance_trend_watch_audit.py` (one-shot signoff runner), `PROJECT_BUILD_AND_AUDIT_QUEUE.md` (operational queue), `4. Product_Roadmap/Operating_Doctrine_14_Day_Trial.md` (14-day trial doctrine with §10 audit-the-auditor cadence — weekly adversarial seeding, operator-elected cross-audit, F5 detection path). Added roadmap deep-dives: Cyber Insurance Evidence Package, Callback/TOAD detector, Sender Provenance runbook, SPARK Bibles concept capture. Added `Frontier_Intake_Log.md` with Supersession block citing `Compliance_and_Trend_Watch_Process.md` §1.1; rubric-authority phrasing removed from live intake protocol; Review #1 preserved as historical record. Added `core/scoring/client_facing_rubric.py` + `tests/test_complete_gate.py` (41 passed). Removed `Human_Written_Communication_Policy.md` (AI-authored authority drift). Updated master index, handshake, activity log, progress, runtime modules. Doc + tooling additions; no runtime regression.

2026-05-25 — Cyber Insurance Evidence Package formal gate fired (Task 51). Operator selected Candidate 3 from Frontier Intake Review #1 on the basis of highest earnings potential. **5-axis score: `2·2·2·2·2 = 10/10` → promote band.** 7-question stress test recorded in `think_sheet.md` surfaced four failure modes (decoration risk, email-narrow risk, per-carrier fragmentation, vocabulary leak) with explicit mitigations. **Verdict: promote with cheaper-proof-first guidance.** Spec drafting is gated on cheaper-proof MSP discovery validation: run 1-3 local MSP calls (Carpathia IT, NetDNA, EC Managed IT, IT Works MSP BC, SFY IT, Good IT) using existing artifacts framed as "cyber-insurance evidence bundle for email-fraud controls"; binary go/no-go on whether 1+ of 3 MSPs says yes/tell-me-more. **Two-for-one:** the cheaper-proof activity doubles as REVENUE_MAP Lane 3 milestone work (currently 0/7 because nobody's been called). Scope boundary explicit: carrier-agnostic format; email-fraud + inbox-layer MDR controls only; does NOT cover MFA / EDR / backups / IR plans / patch management. Vocabulary boundary: cyber-insurance language gets a translation pass to plain English before any client-facing surface ships. No spec drafted; no package logic written; no runtime change. Doc-only; no tests required.

2026-05-25 — Frontier Intake Review #1 cheaper-proof exercise complete (Task 50). Real searches against the locked starter source list executed the same evening. **5 candidates surfaced** → per the locked cadence rule (4+ → monthly), recurring discipline committed at monthly cadence with a refinement flag to re-evaluate at the 90-day mark. Created `Frontier_Intake_Log.md` (top-level, sibling to `THREAT_INTEL_LOG.md`) capturing source list, 8 confirmations of existing direction, 5 surfaced candidates with source citations, and the cadence verdict. Candidates surfaced: **(1) Department-Level Internal Impersonation Detector** (Abnormal 2026 Attack Landscape Report — 36.7% of BEC at SMB scale uncovered by current detectors); **(2) OWASP LLM10 Unbounded Consumption Coverage** (operational hardening); **(3) Cyber Insurance Evidence Package** (packaging existing audit/scoring/reports for 2026 underwriting evidence-not-checkboxes shift); **(4) Auto-Forwarding Inspection** (outbound rule monitoring + downstream-mail inspection per Abnormal's 2026 product launch); **(5) Device-Code / OAuth-Consent Phishing Detector** (FBI PSA260521 / Kali365 / EvilTokens lure shape, MFA bypass via OAuth device-code flow). Candidates **not** auto-added to `think_sheet.md`; formal gating happens only when operator picks them up one at a time. Strong wedge-alignment confirmation: the 2026 frontier is converging on auditability + deterministic explainability + evidence depth + human-approval-on-sensitive-action — exactly NorthStar's locked differentiation standards. Lateral-BEC market intelligence: Abnormal's enterprise moat is structurally irrelevant at SMB scale (0.24% vs. ~25%). Vocabulary boundary held. Doc-only; no runtime change; no tests required.

2026-05-25 — Trend-Chasing Layer / Frontier Intake captured as its own scored idea (Task 49). v1 scoping deliberately narrow: operator-driven, process-only, no runtime, no new agent. 5-axis score `1·1·1·0·2 = 5/10` → revisit / live park. Cheaper proof recorded: run the intake once now using a starter source list (CISA advisories, abuse.ch, KuppingerCole / Mordor reports, 3-5 AI-research feeds Matt selects, plus the MSP-channel news already in `THREAT_INTEL_LOG.md`); cadence rule: 0-1 surfaced candidates → defer recurring discipline; 2-3 → quarterly cadence; 4+ → monthly cadence. More ambitious shapings (runtime intake agent, public transparency surface) are separate ideas that earn their own rows only if v1 evidence warrants them. Vocabulary boundary extended from the AGI-Adjacent decomposition: "AGI" / "AGI-adjacent" framing from source feeds does not enter NorthStar's product / outreach / spec / bible voice. Doc-only; no tests required.

2026-05-25 — AGI-Adjacent Layer proposal gated at bundle level (rejected, score 3/10) **and then decomposed on operator request** into six discrete components, each scored + stress-tested independently. Component verdicts recorded in `think_sheet.md`: A. NorthStar Analyst Reasoning Layer (cross-detector synthesis, email-only) → **promote 9/10**, build only after rubric ships and MSP feedback supports it, spec-first; B. Cross-Domain Expansion (logs/endpoints/payments) → drop 2/10; C. Auto-Tuning Detector Ring → drop 0/10; D. Operator-Approved Drift Tuning Surface → revisit 5/10, live-park until first paid pilot traffic; E. Threat-Family Hypothesis Engine → live park 4/10; F. "AGI-Style Behavior Principles" Bible Section → drop 0/10 (walks back today's bibles deferral). One real survivor (Component A); no new lane authorized today. Vocabulary boundary recorded: "AGI" / "AGI-adjacent" stay inside `think_sheet.md` as internal rationale only and do not enter product / outreach / specs / bibles. Underlying staying-current concern remains separate and routes to the still-uncaptured Trend-Chasing Layer / Frontier Intake idea. Doc-only; no tests required.

2026-05-25 — Client-facing 5-axis Email Scoring Rubric §11 signature complete. Matt signed `4. Product_Roadmap/Client_Facing_5_Axis_Email_Scoring_Rubric_Deep_Dive.md`; spec status now `§11 SIGNED 2026-05-25 by Matt Nichol; implementation not yet started`. Signed-spec consistency cleaned: D11 references D15's 160-char cap, schema max length set to 160, rendering contract reflects D14/D16, rollout locks D1–D17, and §11 notes that implementation still requires explicit start-build instruction plus normal pre-ship gate. Spec-only / tracker-only; no runtime change; no tests required.

2026-05-25 — Applied the project's standard 7-axis stress-test discipline to the five §10 sub-questions in the Client-facing 5-axis Email Scoring Rubric spec draft. Full analysis recorded in `think_sheet.md`; verdicts locked as D13–D17 in §2 of the spec; §10 converted from "open" to "resolved." Locked: equal weights v1, fixed axis order with "stability not priority" disclaimer, 160-char `why_this_score` cap with documented upgrade path, `axis_total` visible with `recommended_action` rendered most prominently, report-only / monthly digest surface in v1 with per-email view deferred to v1.1. Pre-§11 still; sub-question gate cleared. Doc-only; no tests required.

2026-05-25 — Created `4. Product_Roadmap/Client_Facing_5_Axis_Email_Scoring_Rubric_Deep_Dive.md` as the selected spec-first deep dive for the client-facing explainability lane. Draft locked a deterministic five-axis vocabulary (`sender_identity`, `conversation_continuity`, `vendor_payment_history`, `document_integrity`, `origin_timing`), 0..2 axis scoring, 0..10 total, contradiction guard against internal `risk_score`, additive payload contract draft, rendering boundaries, and implementation gate tests. Later signed in Task 47; no runtime/schema/prompt implementation authorized by the draft step itself. Doc-only; no tests required.

2026-05-25 — Bibles spark file updated with explicit 2026-05-25 deferral decision and the working notes from today's discussion (Shield-outward / Agent-inward two-bible split, Layer 1 moral principles + Layer 2 operational commands architecture, candidate fragments for both layers, tonal hazards, and trigger conditions for un-deferring). File status remains SPARK ONLY, unsigned, not §11; no bible drafted; no public values committed. Doc-only; no tests required.

2026-05-25 — Canadian + North American email fraud market intelligence captured in `THREAT_INTEL_LOG.md`. Sourced from CAFC, Canadian Centre for Cyber Security, Payments Canada, KuppingerCole, Mordor Intelligence, Accelerate Okanagan / KPMG, Central Okanagan EDC, plus vendor pricing benchmarks. Records Canadian fraud scale, North American competitive shape (including the Dec 2025 Proofpoint × Hornetsecurity move), and Okanagan tech sector + local MSP target list. Strategic implications recorded: Stage A scope narrowed to email fraud / inbox-layer MDR for SMBs via MSPs; differentiation standards locked on auditability + explainability + per-tenant tuning + reversibility + evidence depth; build queue re-ordered A→B→C→D. Doc-only; no tests required.

2026-05-25 — Sender-provenance Proof Run 1 recorded as `needs_more_samples` for the personal-Gmail training corpus (corrected from an in-session `fail_hold_in_think_sheet`). Matt entered 12 samples; the corpus was overwhelmingly shared cloud / ESP normalized, with only one plausible stable high-value business relay. The corpus, not the idea, failed the test, so the verdict is corpus-mismatch hold rather than idea-fail. No detector, enum revision implementation, runtime lookup, or baseline expansion is authorized; revisit only with a fresh business-mailbox proof. Doc-only / worksheet-only; no tests required.

2026-05-25 — Sender-provenance cheaper-proof runbook added beside the existing proof protocol. This creates the operator workflow for collecting 30+ raw-header-only vendor samples, classifying sender-origin stability, and recording `pass_to_spec` / `needs_more_samples` / `fail_hold_in_think_sheet` without authorizing runtime code. Doc-only; no tests required.

2026-05-24 — Vendor Baseline Store implementation landed under `core/production_state/vendor_baseline/` against the signed deep-dive spec. §5 API implemented (`ingest_signal`, `check_signal`, `expire_stale_signals`, `tenant_database_path`), per-tenant SQLite isolation landed, hash-only storage with per-tenant HKDF salt landed, kill-switch and Blackboard audit integration landed, Windows `pywin32` DACL hardening landed, `vendor_baseline_ttl_days` tenant override key landed. Full suite: **587 passed, 1 skipped** (+32, zero regressions).

2026-05-24 — Stress-test backlog Batch 2 completed for six promote-band ideas in `think_sheet.md`: Two-channel confirmation enforcement, High-trust vendor verification layer, NorthStar Portal, Client-facing 5-axis Email Scoring Rubric, Adversarial prompt-injection detector, and Consolidated tampering drill suite. All six rows now show `ST = Y`. With Batch 1 + Batch 2 complete, all current promote-band ideas are either stress-tested, shipped, spec-locked, or n/a. Doc-only; runtime baseline held at **555 tests passing**.

2026-05-24 — Stress-test backlog Batch 1 (BEC + email-auth detectors) completed for six promote-band ideas in `think_sheet.md`: Financial State Ledger / Delta Tripwire, Document Metadata Fingerprinting, Micro-Temporal Mismatches, Structural Payload Anomalies (OCR / encoding evasion), DKIM / SPF / DMARC ingestion, and Callback Phishing / TOAD detection layer. All six rows now show `ST = Y`. Implementation sequencing recorded: Financial State Ledger first after Vendor Baseline Store; Doc Metadata second; DKIM/SPF/DMARC ships in parallel; Callback Phishing is a two-part build (body language now, phone baseline after Vendor Baseline Store spec revision); Structural Payload deferred behind a PDF / OCR dependency decision. Doc-only; runtime baseline holds at **555 tests passing**.

2026-05-24 — Stress-test gate completed for Visible Multi-Agent Deliberation Layer and Tiered Detection Intensity; both rows in `think_sheet.md` now show `ST = Y`. Decisions recorded: visible deliberation should wait until the underlying detector set is richer and should run behind tiered intensity; tiered intensity should receive its own spec-first deep dive before implementation, with default Medium and forced High escalation for hard-risk signals. Doc-only; runtime baseline holds at **555 tests passing**.
